using System;

namespace Database
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

            /*
             * comparar ADO VS DAPPER VS ENTITY FRAMEWORK
             * comparar bulk insert, select, update
             * comparar com sql server e postgresql
             * 
             */
        }
    }
}
